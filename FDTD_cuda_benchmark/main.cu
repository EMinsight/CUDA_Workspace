#define _USE_MATH_DEFINES
#include <iostream>
#include <cmath>
#include <fstream>
#include <string>
#include <cuda.h>
#include <cuda_runtime.h>

int main( void ){

    std::ofstream ofs;

    ofs.open("./result/Magnitude.dat");

    int Num_obs = ( )

    return 0;
}